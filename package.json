{
  "name": "agentraining-site",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --import ./tests/register.mjs --test tests/*.test.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.13",
    "@netlify/identity": "^1.2.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
