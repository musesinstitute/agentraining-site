import { getStore } from '@netlify/blobs';
import { getUser, verifyRequestOrigin } from '@netlify/identity';

const STORE_NAME='agentraining-pilot',BATCH_SIZE=5,TIMEOUT_MS=30000,VERIFY_TIMEOUT_MS=30000;
const headers={'content-type':'application/json; charset=utf-8','cache-control':'no-store'};
const reply=(s,b)=>new Response(JSON.stringify(b),{status:s,headers});
const clean=(v,n=500)=>String(v??'').trim().slice(0,n);
const seg=(v,f='founding-pilot')=>clean(v,100).toLowerCase().replace(/[^a-z0-9_-]+/g,'-').replace(/^-+|-+$/g,'')||f;
const fp=v=>clean(v,1400).toLowerCase().replace(/[^a-z0-9\u3400-\u9fff]+/g,' ').replace(/\s+/g,' ').trim();
function text(p){if(typeof p?.output_text==='string')return p.output_text.trim();const a=[];for(const x of p?.output||[])for(const c of x?.content||[])if(c?.type==='output_text'&&c?.text)a.push(c.text);return a.join('\n').trim()}
function json(raw){let t=String(raw||'').trim().replace(/^```(?:json)?\s*/i,'').replace(/```\s*$/,'').trim();try{return JSON.parse(t)}catch{}const a=t.indexOf('{'),b=t.lastIndexOf('}');if(a>=0&&b>a)try{return JSON.parse(t.slice(a,b+1))}catch{}throw Object.assign(new Error('AI returned unreadable JSON. Retry this batch.'),{status:502})}
async function actor(req){verifyRequestOrigin(req);const u=await getUser(req);if(!u)throw Object.assign(new Error('Please sign in.'),{status:401});const r=Array.isArray(u.roles)?u.roles:[];if(!r.includes('manager')&&!r.includes('admin'))throw Object.assign(new Error('Manager access is required.'),{status:403});return{teamId:seg(u.appMetadata?.team_id),email:clean(u.email,254).toLowerCase()}}
function phaseFor(position){if(position<=20)return{key:'broad_coverage',depths:['Recall','Understanding'],types:['mcq','truefalse','scenario']};if(position<=30)return{key:'understanding_application',depths:['Understanding','Application'],types:['mcq','scenario']};return{key:'application_reinforcement',depths:['Application','Understanding'],types:['scenario','mcq']}}
function allowedDepths(point,phase){const eligible=point.eligibleDepths?.length?point.eligibleDepths:['Recall','Understanding','Application'];const preferred=phase.depths.filter(d=>eligible.includes(d));return preferred.length?preferred:(eligible.filter(d=>d!=='Recall').length?eligible.filter(d=>d!=='Recall'):eligible)}
function rejectionPressure(bank,id){const h=bank?.adaptive?.pointRejects||{};return Number(h[id]||0)}
function choosePoints(map,current,count,startPosition,bank){const used=new Map();for(const q of current){const s=used.get(q.knowledgePointId)||new Set();s.add(q.depth||'Understanding');used.set(q.knowledgePointId,s)}const points=Array.isArray(map.points)?map.points:[],selected=[];for(let offset=0;offset<count;offset++){const position=startPosition+offset,phase=phaseFor(position),preferUnused=position<=20;let pool=points.filter(p=>{const seen=used.get(p.id)||new Set();return (preferUnused?!used.has(p.id):allowedDepths(p,phase).some(d=>!seen.has(d)))&&!selected.some(s=>s.point.id===p.id)});if(!pool.length)pool=points.filter(p=>!selected.some(s=>s.point.id===p.id)&&allowedDepths(p,phase).some(d=>!(used.get(p.id)||new Set()).has(d)));pool.sort((a,b)=>rejectionPressure(bank,a.id)-rejectionPressure(bank,b.id));const p=pool[0];if(!p)break;const seen=used.get(p.id)||new Set(),depths=allowedDepths(p,phase),preferredDepth=depths.find(d=>!seen.has(d))||depths[0];selected.push({point:p,phase,preferredDepth,retryCount:rejectionPressure(bank,p.id)});seen.add(preferredDepth);used.set(p.id,seen)}return selected}
function normalize(q,i,a){const {point,phase,preferredDepth}=a,type=['mcq','truefalse','scenario'].includes(q?.type)?q.type:'mcq',allowed=allowedDepths(point,phase),depth=allowed.includes(q?.depth)?q.depth:preferredDepth||allowed[0]||'Understanding';let options=Array.isArray(q?.options)?q.options.slice(0,4).map(x=>clean(x,500)).filter(Boolean):[],answer=clean(q?.answer,40);if(type==='truefalse'){options=['True','False'];const z=answer.toLowerCase();answer=(z==='true'||z==='a'||z==='1')?'True':(z==='false'||z==='b'||z==='0')?'False':answer}return{id:i,type,difficulty:['Basic','Intermediate','Advanced'].includes(q?.difficulty)?q.difficulty:'Basic',depth,trainingPhase:phase.key,knowledgePointId:point.id,knowledgePointTitle:point.label,question:clean(q?.question,1200),options,answer,explanation:clean(q?.explanation,1200),sourceReference:clean(q?.sourceReference||point.sourceReference,300),sensitiveClaim:!!point.sensitiveClaim}}
function validate(q,current,phase){const reasons=[];if(!q.question||q.question.length<12)reasons.push('question_too_short');if(!q.sourceReference)reasons.push('missing_source_reference');if(!q.explanation||q.explanation.length<8)reasons.push('missing_explanation');if(phase.key!=='broad_coverage'&&!phase.depths.includes(q.depth))reasons.push('wrong_training_depth_for_phase');if(phase.key==='understanding_application'&&q.type==='truefalse')reasons.push('shallow_format_after_q20');if(q.type==='truefalse'){if(!['True','False'].includes(q.answer)||q.options.length!==2)reasons.push('invalid_truefalse_format')}else{const a=q.answer.toUpperCase(),idx=/^[A-D]$/.test(a)?a.charCodeAt(0)-65:-1;if(q.options.length<2||idx<0||idx>=q.options.length)reasons.push('invalid_answer_options')}if(current.some(x=>fp(x.question)===fp(q.question)))reasons.push('exact_duplicate');if(current.some(x=>x.knowledgePointId===q.knowledgePointId&&(x.depth||'Understanding')===q.depth))reasons.push('same_canonical_point_same_depth');return reasons}
async function aiCall(input,instructions,maxOutput=3000,timeout=TIMEOUT_MS){const key=process.env.OPENAI_API_KEY;if(!key)throw Object.assign(new Error('Question Bank AI is not configured.'),{status:503});const model=process.env.OPENAI_KNOWLEDGE_MODEL||process.env.OPENAI_CHAT_MODEL||'gpt-5.4-mini',controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeout);try{const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',signal:controller.signal,headers:{'content-type':'application/json',authorization:`Bearer ${key}`},body:JSON.stringify({model,instructions,input,max_output_tokens:maxOutput})});const b=await r.json().catch(()=>({}));if(!r.ok)throw Object.assign(new Error(b?.error?.message||`AI provider returned ${r.status}.`),{status:r.status>=500?503:502});if(b?.status==='incomplete')throw Object.assign(new Error('AI response was incomplete. Accepted questions remain safe; retry.'),{status:502});return{parsed:json(text(b)),model}}catch(e){if(e?.name==='AbortError')throw Object.assign(new Error('AI quality check took too long. Accepted questions remain safe; retry.'),{status:503});throw e}finally{clearTimeout(timer)}}
async function generate(assignments,difficulty,current,bank){const rows=assignments.map(a=>({id:a.point.id,title:a.point.label,summary:a.point.summary,sourceReference:a.point.sourceReference,sensitiveClaim:!!a.point.sensitiveClaim,requiredPhase:a.phase.key,requiredDepth:a.preferredDepth,allowedTypes:a.phase.types,retryCount:a.retryCount}));const recent=(bank?.quality?.lastRejected||[]).slice(-10);const input=['Generate exactly one enterprise training question for each assignment.','ADAPTIVE RETRY: if retryCount > 0, do NOT repeat the previous construction. Change the cognitive operation and wording. Prefer a different allowed question type.','Q21-30 must require Understanding or Application: compare, interpret, choose, calculate, or apply. Never use True/False.','Q31-50 prioritize grounded application/scenario reinforcement.','Use only the assigned canonical point. Preserve source direction, numbers, qualifiers, exceptions and scope exactly. Never correct the source using outside insurance knowledge.','Sensitive claims must explicitly say according to this source/material.','Answer, options and explanation must be internally consistent and unambiguous.','ASSIGNMENTS:\n'+JSON.stringify(rows),'RECENT REJECTIONS TO AVOID:\n'+JSON.stringify(recent),'PRIOR ACCEPTED:\n'+JSON.stringify(current.slice(-50).map(q=>({k:q.knowledgePointId,depth:q.depth,type:q.type,q:q.question}))),'Difficulty: '+difficulty,'Return ONLY JSON: {"questions":[{"knowledgePointId":"K001","type":"mcq|truefalse|scenario","difficulty":"Basic|Intermediate|Advanced","depth":"Recall|Understanding|Application","question":"...","options":["..."],"answer":"A|B|C|D|True|False","explanation":"...","sourceReference":"..."}]}'].join('\n\n');const out=await aiCall(input,'Compact valid JSON only. Every Question Must Earn Its Place.',3200);return{rows:Array.isArray(out.parsed?.questions)?out.parsed.questions:[],model:out.model}}
async function verifyCandidates(candidates,assignments,current){if(!candidates.length)return{accepted:[],rejected:[]};const byPoint=new Map(assignments.map(a=>[a.point.id,a]));const payload=candidates.map((q,i)=>({candidateId:i,question:q,canonicalPoint:byPoint.get(q.knowledgePointId)?.point||null,phase:byPoint.get(q.knowledgePointId)?.phase||null}));const input=['Independent enterprise Question Quality Gate.','Accept only when K-ID, answer/options/explanation, arithmetic/logic, source fidelity, semantic uniqueness and format are all sound.','For understanding_application reject direct recall, definition recognition or paraphrase. Require comparison, interpretation, calculation, choice or application.','Judge source fidelity against the canonical point, not outside knowledge. Ambiguous support => needs_review. Sensitive claims must be source-framed.','CANDIDATES:\n'+JSON.stringify(payload),'PRIOR ACCEPTED:\n'+JSON.stringify(current.slice(-80).map(q=>({knowledgePointId:q.knowledgePointId,depth:q.depth,question:q.question}))),'Return ONLY JSON: {"verdicts":[{"candidateId":0,"status":"accepted|rejected_duplicate|rejected_logic|rejected_grounding|rejected_sensitive_claim|rejected_wrong_knowledge_point|rejected_depth|rejected_format|needs_review","reason":"brief reason"}]}'].join('\n\n');const out=await aiCall(input,'Compact valid JSON only. Be conservative.',2600,VERIFY_TIMEOUT_MS),vs=Array.isArray(out.parsed?.verdicts)?out.parsed.verdicts:[],vm=new Map(vs.map(v=>[Number(v.candidateId),v])),accepted=[],rejected=[];candidates.forEach((q,i)=>{const v=vm.get(i);if(v?.status==='accepted')accepted.push(q);else rejected.push({knowledgePointId:q.knowledgePointId,question:clean(q.question,220),reasons:[clean(v?.status||'needs_review',80)],reviewReason:clean(v?.reason||'Verifier did not accept.',300)})});return{accepted,rejected}}

export default async req=>{if(req.method!=='POST')return reply(405,{error:'POST required.'});try{const a=await actor(req),input=await req.json().catch(()=>({})),knowledgeId=clean(input.knowledgeId||input.id,100),action=clean(input.action,40)||'start';if(!knowledgeId)return reply(400,{error:'knowledgeId is required.'});const store=getStore({name:STORE_NAME,consistency:'strong'}),prefix=`teams/${a.teamId}`,source=await store.get(`${prefix}/knowledge/${knowledgeId}`,{type:'json'}),map=await store.get(`${prefix}/knowledge-maps/${knowledgeId}`,{type:'json'});if(!source)return reply(404,{error:'Knowledge source not found.'});if(!source.consentConfirmed)return reply(400,{error:'Confirm organizational authorization and AI processing consent first.'});if(!map?.points?.length)return reply(409,{error:'Build the canonical Knowledge Map before generating questions.',needsKnowledgeMap:true});if(action==='start'){const target=Math.min(Math.max(parseInt(input.count)||20,5),100),difficulty=['Basic','Intermediate','Advanced','Mixed'].includes(input.difficulty)?input.difficulty:'Mixed',id=crypto.randomUUID(),now=new Date().toISOString(),bank={id,engineVersion:2,depthProgressionVersion:2,qualityGateVersion:5,adaptiveRegenerationVersion:1,knowledgeId,knowledgeMapId:map.id,knowledgeMapVersion:map.version||1,teamId:a.teamId,title:clean(source.title,240)+' — Question Bank',difficulty,targetQuestions:target,totalQuestions:0,batchSize:BATCH_SIZE,status:'generating',questions:[],coverage:{availableKnowledgePoints:map.points.length,coveredKnowledgePoints:0,percent:0},quality:{accepted:0,rejected:0,reasons:{},lastRejected:[]},adaptive:{pointRejects:{}},createdAt:now,updatedAt:now,createdBy:a.email};await store.setJSON(`${prefix}/question-banks/${id}`,bank);return reply(200,{questionBank:bank,next:{action:'generate_batch',bankId:id}})}const bankId=clean(input.bankId,100);if(!bankId)return reply(400,{error:'bankId is required.'});const key=`${prefix}/question-banks/${bankId}`;let bank=await store.get(key,{type:'json'});if(!bank||bank.knowledgeId!==knowledgeId)return reply(404,{error:'Question Bank not found.'});if(action==='status')return reply(200,{questionBank:bank});if(action!=='generate_batch')return reply(400,{error:'Unsupported action.'});if(bank.status==='complete')return reply(200,{questionBank:bank,complete:true});const current=Array.isArray(bank.questions)?bank.questions:[],remaining=Math.max(0,bank.targetQuestions-current.length);if(!remaining)return reply(200,{questionBank:{...bank,status:'complete'},complete:true});const assignments=choosePoints(map,current,Math.min(BATCH_SIZE,remaining),current.length+1,bank);if(!assignments.length)return reply(409,{error:'No grounded canonical knowledge points remain for safe generation. Existing accepted questions are saved.',questionBank:bank});const out=await generate(assignments,bank.difficulty,current,bank),byId=new Map(assignments.map(a=>[a.point.id,a])),det=[],rejected=[];for(const row of out.rows){const asg=byId.get(clean(row?.knowledgePointId,20));if(!asg){rejected.push({reasons:['wrong_or_unknown_knowledge_point']});continue}const q=normalize(row,current.length+det.length+1,asg),reasons=validate(q,[...current,...det],asg.phase);if(reasons.length)rejected.push({knowledgePointId:asg.point.id,question:clean(q.question,220),reasons});else det.push(q)}const verified=await verifyCandidates(det,assignments,current);rejected.push(...verified.rejected);const accepted=verified.accepted,merged=[...current,...accepted].map((q,i)=>({...q,id:i+1})),covered=new Set(merged.map(q=>q.knowledgePointId).filter(Boolean)),reasonCounts={...(bank.quality?.reasons||{})},pointRejects={...(bank.adaptive?.pointRejects||{})};for(const r of rejected){for(const reason of r.reasons||[])reasonCounts[reason]=(reasonCounts[reason]||0)+1;if(r.knowledgePointId)pointRejects[r.knowledgePointId]=(pointRejects[r.knowledgePointId]||0)+1}for(const q of accepted)if(pointRejects[q.knowledgePointId])pointRejects[q.knowledgePointId]=Math.max(0,pointRejects[q.knowledgePointId]-1);const now=new Date().toISOString();bank={...bank,adaptiveRegenerationVersion:1,questions:merged,totalQuestions:merged.length,status:merged.length>=bank.targetQuestions?'complete':'generating',coverage:{availableKnowledgePoints:map.points.length,coveredKnowledgePoints:covered.size,percent:Math.round(covered.size/Math.max(1,map.points.length)*100)},quality:{accepted:(bank.quality?.accepted||0)+accepted.length,rejected:(bank.quality?.rejected||0)+rejected.length,reasons:reasonCounts,lastRejected:rejected.slice(-10)},adaptive:{pointRejects},model:out.model,updatedAt:now,lastSuccessfulBatchAt:accepted.length?now:bank.lastSuccessfulBatchAt};await store.setJSON(key,bank);return reply(200,{questionBank:bank,complete:bank.status==='complete',acceptedThisBatch:accepted.length,rejectedThisBatch:rejected.length})}catch(e){console.error('knowledge-question-bank-v2',e);return reply(e.status||500,{error:e.message||'Question Bank generation failed.'})}};
